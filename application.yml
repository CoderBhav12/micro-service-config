eureka:
  instance:
    prefer-ip-address: true 
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
info:
  app:
    name: application
    description: It is a microservice application
spring:
  config:
    activate:
      on-profile: default
  
  data:
    mongodb:  
      host: localhost
      port: 27017
      database: MICROSERVICES-LIVE
